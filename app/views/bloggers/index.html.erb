<h1>Bloggers</h1>
<ul>
  <% @bloggers.each do |blogger| %>
    <li>
      <%= link_to blogger.name, blogger_path(blogger) %>
      (<%= blogger.posts.count %> <%= "posts".pluralize(blogger.posts.count) %>)
    </li>
  <% end %>
</ul>